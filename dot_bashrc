# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
alias grep='grep --color=auto'
PS1='[\u@\h \W]\$ '
# Onyx config
export ONYX_PATH="/home/rond/.onyx"
export PATH="$ONYX_PATH/bin:$PATH"
. "$HOME/.cargo/env"
. "/home/rond/.deno/env"
